# Docker environment — copy this to .env for docker-compose or use as-is
# For Swarm, use docker secret create instead of env vars for secrets

MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=dyo
MYSQL_USER=dyo
MYSQL_PASSWORD=dyo_pass

DATABASE_URL=mysql://dyo:dyo_pass@mysql:3306/dyo
JWT_SECRET=devid-porto-jwt-secret-key-2024
JWT_EXPIRES_IN=7d
PORT=5000
NODE_ENV=production
